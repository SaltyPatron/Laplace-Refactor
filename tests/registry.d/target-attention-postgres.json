{
  "schema": "laplace.test-registry-fragment/v1",
  "tests": [
    {
      "ctest_name": "postgres.target-attention-native-parity",
      "profiles": ["postgres", "custom-stack"],
      "evidence_targets": [
        "LP-TEST-TYPED-INCIDENCE-OPERATOR",
        "LP-TEST-MATRIX-FREE-MATERIALIZED-PARITY",
        "LP-TEST-CROSS-LAYER-DISTINCTION-PRESERVATION"
      ],
      "covered_dimensions": [
        "PostgreSQL submits the complete paired QK/VO target job set in one native call",
        "SQL receives the generated shared embedding and Q/K/V/O tensors rather than application-side filler",
        "PostgreSQL and native target compilation produce identical compile projection embedding and head receipts",
        "insufficient target hidden width fails closed instead of cycling or random filling semantic capacity"
      ]
    },
    {
      "ctest_name": "postgres.target-attention-safetensors-export",
      "profiles": ["postgres", "custom-stack"],
      "evidence_targets": [
        "LP-TEST-TYPED-INCIDENCE-OPERATOR",
        "LP-TEST-MATRIX-FREE-MATERIALIZED-PARITY",
        "LP-TEST-CROSS-LAYER-DISTINCTION-PRESERVATION"
      ],
      "covered_dimensions": [
        "the legacy explicit-job PostgreSQL export remains deterministic and compatible",
        "the scoped PostgreSQL export accepts one shared typed field/constraint estate plus QK/VO selectors and delegates job/program identity generation to native target-scope planning",
        "native and PostgreSQL scoped planning produce identical scope compile projection and embedding receipts without client-authored operator identities",
        "the scoped result retains the scope receipt selected-constraint count source mask compile projection and SafeTensors artifact provenance"
      ]
    },
    {
      "ctest_name": "postgres.target-attention-model-export-cli",
      "profiles": ["postgres", "custom-stack"],
      "evidence_targets": [
        "LP-TEST-TYPED-INCIDENCE-OPERATOR",
        "LP-TEST-MATRIX-FREE-MATERIALIZED-PARITY",
        "LP-TEST-CROSS-LAYER-DISTINCTION-PRESERVATION"
      ],
      "covered_dimensions": [
        "the installed laplace-model-export v2 client accepts one shared typed estate plus declarative target slots instead of repeated target_operator_job records",
        "the client renders one scoped PostgreSQL operation and does not reconstruct operator_program or target-job semantic identities",
        "the client writes an actual SafeTensors file and a scope/compile/projection provenance receipt and validates the returned artifact before publication",
        "a second invocation over identical closed state produces byte-identical artifact bytes and the same native scope compile and projection identities"
      ]
    }
  ]
}

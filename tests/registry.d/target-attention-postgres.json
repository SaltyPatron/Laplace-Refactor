{
  "schema": "laplace.test-registry-fragment/v1",
  "tests": [
    {
      "ctest_name": "postgres.target-attention-native-parity",
      "profiles": ["postgres", "custom-stack"],
      "evidence_targets": [
        "LP-TEST-TYPED-INCIDENCE-OPERATOR",
        "LP-TEST-MATRIX-FREE-MATERIALIZED-PARITY",
        "LP-TEST-CROSS-LAYER-DISTINCTION-PRESERVATION"
      ],
      "covered_dimensions": [
        "PostgreSQL submits the complete paired QK/VO target job set in one native call",
        "SQL receives the generated shared embedding and Q/K/V/O tensors rather than application-side filler",
        "PostgreSQL and native target compilation produce identical compile projection embedding and head receipts",
        "insufficient target hidden width fails closed instead of cycling or random filling semantic capacity"
      ]
    },
    {
      "ctest_name": "postgres.target-attention-safetensors-export",
      "profiles": ["postgres", "custom-stack"],
      "evidence_targets": [
        "LP-TEST-TYPED-INCIDENCE-OPERATOR",
        "LP-TEST-MATRIX-FREE-MATERIALIZED-PARITY",
        "LP-TEST-CROSS-LAYER-DISTINCTION-PRESERVATION"
      ],
      "covered_dimensions": [
        "PostgreSQL performs operator compilation projection and SafeTensors materialization in one user-callable server operation",
        "the returned artifact contains the derived shared embedding plus actual Q K V and O tensors",
        "artifact metadata retains compile projection operator factorization head and closed-epoch provenance",
        "identical closed state produces byte-identical artifacts and insufficient hidden width fails before publication"
      ]
    },
    {
      "ctest_name": "postgres.target-attention-model-export-cli",
      "profiles": ["postgres", "custom-stack"],
      "evidence_targets": [
        "LP-TEST-TYPED-INCIDENCE-OPERATOR",
        "LP-TEST-MATRIX-FREE-MATERIALIZED-PARITY",
        "LP-TEST-CROSS-LAYER-DISTINCTION-PRESERVATION"
      ],
      "covered_dimensions": [
        "the installed laplace-model-export client accepts one explicit JSON recipe and submits one set-wise PostgreSQL export request",
        "the client writes an actual SafeTensors file and separate provenance receipt rather than returning internal tensor arrays",
        "a second invocation over identical closed state produces byte-identical artifact bytes and the same native compile/projection identities",
        "the client independently validates SafeTensors metadata tensor inventory and byte accounting before publishing files"
      ]
    }
  ]
}
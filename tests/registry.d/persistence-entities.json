{
  "schema": "laplace.test-registry/v1",
  "tests": [
    {
      "ctest_name": "persistence.entity-producer-native-contract",
      "profiles": ["postgres", "custom-stack"],
      "evidence_targets": [
        "LP-TEST-IDENTITY-CROSS-ROUTE",
        "LP-TEST-IDENTITY-NEGATIVE",
        "LP-TEST-BULK-DETERMINISM"
      ],
      "covered_dimensions": [
        "actual entity adapter emits only sorted unique shared native persistence entity frames",
        "real framework producer and stream receipts bind context source recipe and finite batch partition",
        "same canonical stream survives batch partition and exact producer replay",
        "duplicate reversed and mismatched identities and invalid memory layouts reject",
        "native checking sink is separate from live PostgreSQL deposition acceptance"
      ]
    }
  ]
}

name: postgresql-live-provider-proof

on:
  push:
    branches:
      - 'delivery/postgres-live-*'

permissions:
  contents: read

concurrency:
  group: postgresql-live-provider-proof-${{ github.ref }}
  cancel-in-progress: true

jobs:
  postgresql-product:
    uses: ./.github/workflows/postgresql-product.yml

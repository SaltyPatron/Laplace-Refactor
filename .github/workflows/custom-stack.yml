name: custom-stack-ci

on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: custom-stack-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'PR86 is superseded; release the self-hosted runner.'

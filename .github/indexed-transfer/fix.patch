diff --git a/state/continuation.json b/state/continuation.json
index d4dd2eb4..bc9a6f53 100644
--- a/state/continuation.json
+++ b/state/continuation.json
@@ -1,4 +1,18 @@
 {
+  "indexed_cognition_repair_20260907": {
+    "classification": "observed-development-state-not-product-law",
+    "owning_pull_request": 253,
+    "base_commit": "84d246ebac62a0ed52d0cec2410bb309be756c7a",
+    "tested_source": "e2517e9de3b30989b7196678adbccc799038b130",
+    "physical_acceptance_run": 34165296097,
+    "observed_behavior": "Native C-to-B-to-A structural cognition executes over persisted physicalities in two frontier batches and four database calls. Adding 4096 unrelated native-built physicalities leaves its answer, receipts, two fetched records and 192 trajectory bytes unchanged.",
+    "observed_index_plan": "Bitmap GIN lookup returns one matching row with four shared-buffer hits immediately after ingestion, without ANALYZE, VACUUM or planner overrides. The earlier default-pending-list plan scanned all 4097 rows.",
+    "negative_controls": "Corrupt matched trajectories, missing provider workspace, exhausted database budget and attempts to label physicality as semantic testimony are rejected; exact execution succeeds again after error cleanup.",
+    "native_tests": "71 tests across 15 suites passed, including all prompt-composition and candidate-batch cases.",
+    "deployment_baseline": "Main 84d246ebac62a0ed52d0cec2410bb309be756c7a completed persistent product activation and readback in run 34163089636. PR254, PR261 and PR262 are merged; their older pending checkpoints below are historical.",
+    "remaining_acceptance": "Current PR253 hosted native/sanitizer/requirements checks and ordinary main deployment must still pass before installed indexed cognition is claimed.",
+    "product_scope": "This structural persistence provider and additive 1.0.1 upgrade do not close testimony, general prompt-goal compilation, firmware/Godel execution, configured seed or public multimodal product requirements."
+  },
   "highway_replay_repair_20260907": {
     "classification": "observed-development-state-not-product-law",
     "base_commit": "d47ff2d7898db2b15fcce703628545ea15224f27",
diff --git a/tests/indexed_cognition_upgrade_tests.py b/tests/indexed_cognition_upgrade_tests.py
index c0169ffc..1d15473a 100644
--- a/tests/indexed_cognition_upgrade_tests.py
+++ b/tests/indexed_cognition_upgrade_tests.py
@@ -35,7 +35,8 @@ class IndexedCognitionUpgrade(unittest.TestCase):
         for part in ("BEGIN;", "COMMIT;", "version = '1.0.0'", "version <> '1.0.1'",
                      "ALTER EXTENSION laplace UPDATE TO '1.0.1'", "owner <> current_user",
                      "d.deptype='e'", "p.probin='laplace_pg'", "p.prosrc=target.symbol",
-                     "NOT p.prosecdef", "i.indisvalid AND i.indisready"):
+                     "NOT p.prosecdef", "i.indisvalid AND i.indisready",
+                     "a.amname='gin'", "'fastupdate=off'=ANY(c.reloptions)"):
             self.assertIn(part,program)
         self.assertEqual(receipt['package_id'],self.package['package_id'])
         self.assertEqual(receipt['receipt_sha256'],runner.document_identity(receipt,'receipt_sha256'))
diff --git a/tools/delivery/product_activation_runner.py b/tools/delivery/product_activation_runner.py
index 56c58f98..7fea862d 100644
--- a/tools/delivery/product_activation_runner.py
+++ b/tools/delivery/product_activation_runner.py
@@ -293,6 +293,14 @@ BEGIN
           AND i.indisvalid AND i.indisready) <> 2 THEN
         RAISE EXCEPTION 'indexed cognition indexes are not ready';
     END IF;
+    IF NOT EXISTS (SELECT 1 FROM pg_catalog.pg_class c
+                   JOIN pg_catalog.pg_index i ON i.indexrelid=c.oid
+                   JOIN pg_catalog.pg_am a ON a.oid=c.relam
+                   WHERE c.oid='laplace.physicality_constituent_lookup_idx'::regclass
+                     AND i.indrelid='laplace.physicality'::regclass
+                     AND a.amname='gin' AND 'fastupdate=off'=ANY(c.reloptions)) THEN
+        RAISE EXCEPTION 'indexed cognition membership requires its maintained GIN posting tree';
+    END IF;
 END $migration$;
 SELECT pg_catalog.json_build_object('schema','laplace.indexed-cognition-upgrade/v1',
     'version',extversion,'owner',current_user,'native_bindings',2,'ready_indexes',2)::text

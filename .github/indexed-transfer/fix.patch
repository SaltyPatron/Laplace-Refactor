diff --git a/engine/include/laplace/cognition_observation_request.h b/engine/include/laplace/cognition_observation_request.h
index 688e3c1a..a5dc6afb 100644
--- a/engine/include/laplace/cognition_observation_request.h
+++ b/engine/include/laplace/cognition_observation_request.h
@@ -26,6 +26,8 @@ enum {
         LAPLACE_COGNITION_OBSERVATION_REQUEST_ALLOW_TYPED_UNRESOLVED |
         LAPLACE_COGNITION_OBSERVATION_REQUEST_BOUNDARY_COMPLETE,
     LAPLACE_COGNITION_OBSERVATION_REQUEST_VERSION = 1,
+    /* Candidate projection owns at most two candidate-sized arrays per slot. */
+    LAPLACE_COGNITION_OBSERVATION_CANDIDATE_WORKSPACE_MULTIPLIER = 2,
     /* Candidate payload grew an explicit canonical relation identity/direction. */
     LAPLACE_COGNITION_OBSERVATION_CANDIDATE_PROVIDER_ABI_MAJOR = 2,
     LAPLACE_COGNITION_OBSERVATION_CANDIDATE_PROVIDER_ABI_MINOR = 0,
@@ -157,6 +159,9 @@ typedef struct laplace_cognition_observation_candidate_provider_v1 {
  * No search state, completion decision or testimony is manufactured here.
  * Output is atomic: insufficient candidate/work capacity returns OVERFLOW with
  * zero published candidates. Empty source arrays are valid no-op batches.
+ * Transient candidate storage is bounded by WORKSPACE_MULTIPLIER times
+ * candidate_capacity times sizeof(laplace_cognition_observation_candidate),
+ * separately from the immutable index and caller-owned output storage.
  */
 LAPLACE_API laplace_observation_query_status
 laplace_observation_query_index_candidates_batch(
diff --git a/engine/src/cognition_observation_index_candidates.inc b/engine/src/cognition_observation_index_candidates.inc
index a7ec19cf..c14e0184 100644
--- a/engine/src/cognition_observation_index_candidates.inc
+++ b/engine/src/cognition_observation_index_candidates.inc
@@ -1,5 +1,25 @@
 #include "laplace/cognition_observation_request.h"
 #include <stdexcept>
+#include <memory>
+
+namespace laplace_observation_query_detail {
+
+// Fixed storage for the same canonical generator. No growth occurs before an
+// output-capacity failure, and no caller buffer is written before whole success.
+struct CandidateSpan final {
+    Candidate* storage{};
+    std::size_t used{};
+    void clear() { used = 0U; }
+    std::size_t size() const { return used; }
+    void push_back(const Candidate& value) { storage[used++] = value; }
+    Candidate* begin() { return storage; }
+    Candidate* end() { return used == 0U ? storage : storage + used; }
+};
+
+static_assert(sizeof(Candidate) <= sizeof(laplace_cognition_observation_candidate),
+              "candidate projection exceeds its published workspace law");
+
+}  // namespace laplace_observation_query_detail
 
 /* Shared projection, not a second structural/semantic engine. GenerateCandidates
  * owns ordered occurrence/run, containment, direction and co-occurrence laws. */
@@ -23,6 +43,7 @@ laplace_observation_query_index_candidates_batch(
     }
     if (source_count > std::numeric_limits<std::size_t>::max() / sizeof(laplace_id128) ||
         candidate_capacity > std::numeric_limits<std::size_t>::max() /
+            LAPLACE_COGNITION_OBSERVATION_CANDIDATE_WORKSPACE_MULTIPLIER /
             sizeof(laplace_cognition_observation_candidate)) {
         return LAPLACE_OBSERVATION_QUERY_OVERFLOW;
     }
@@ -31,18 +52,26 @@ laplace_observation_query_index_candidates_batch(
         !SameDigest(identified, binding->binding_fingerprint)) {
         return LAPLACE_OBSERVATION_QUERY_BINDING_INVALID;
     }
-    std::vector<laplace_cognition_observation_candidate> staged;
+    if (source_count == 0U) return LAPLACE_OBSERVATION_QUERY_OK;
+    std::unique_ptr<laplace_cognition_observation_candidate[]> staged;
+    std::unique_ptr<Candidate[]> generated_storage;
+    std::size_t staged_count = 0U;
     laplace_cognition_observation_candidate_usage measured{};
     try {
+        if (candidate_capacity != 0U) {
+            staged.reset(new laplace_cognition_observation_candidate[candidate_capacity]);
+            generated_storage.reset(new Candidate[candidate_capacity]);
+        }
         for (std::size_t source_index = 0U; source_index < source_count; ++source_index) {
-            std::vector<Candidate> generated;
+            CandidateSpan generated{generated_storage.get(), 0U};
             std::uint64_t examined = 0U, plans = 0U;
             bool limited = false;
             if (!GenerateCandidates(*index, *binding, source_entity_ids[source_index],
-                                    &generated, &examined, &plans, &limited)) {
+                                    &generated, &examined, &plans, &limited,
+                                    candidate_capacity - staged_count)) {
                 return LAPLACE_OBSERVATION_QUERY_TRAJECTORY_INVALID;
             }
-            if (limited || generated.size() > candidate_capacity - staged.size() ||
+            if (limited || generated.size() > candidate_capacity - staged_count ||
                 !Add(measured.rows_examined, examined, &measured.rows_examined) ||
                 !Add(measured.index_plan_count, plans, &measured.index_plan_count)) {
                 return LAPLACE_OBSERVATION_QUERY_OVERFLOW;
@@ -75,7 +104,7 @@ laplace_observation_query_index_candidates_batch(
                     default:
                         return LAPLACE_OBSERVATION_QUERY_BINDING_INVALID;
                 }
-                staged.push_back(projected);
+                staged[staged_count++] = projected;
             }
         }
     } catch (const std::bad_alloc&) {
@@ -83,11 +112,11 @@ laplace_observation_query_index_candidates_batch(
     } catch (const std::length_error&) {
         return LAPLACE_OBSERVATION_QUERY_OVERFLOW;
     }
-    measured.crossing_count = static_cast<std::uint64_t>(staged.size());
-    if (!staged.empty()) {
-        std::copy(staged.begin(), staged.end(), candidates);
+    measured.crossing_count = static_cast<std::uint64_t>(staged_count);
+    if (staged_count != 0U) {
+        std::copy_n(staged.get(), staged_count, candidates);
     }
-    *candidate_count = staged.size();
+    *candidate_count = staged_count;
     *usage = measured;
     return LAPLACE_OBSERVATION_QUERY_OK;
 }
diff --git a/engine/src/observation_query_part01.inc b/engine/src/observation_query_part01.inc
index b0b7fb8c..1b7ce0db 100644
--- a/engine/src/observation_query_part01.inc
+++ b/engine/src/observation_query_part01.inc
@@ -31,9 +31,10 @@ bool ChargeWork(
     return true;
 }
 
+template<class CandidateSequence>
 bool AppendCandidate(
     const laplace_observation_query_index& index,
-    std::vector<Candidate>* const candidates,
+    CandidateSequence* const candidates,
     const Candidate& candidate,
     bool* const limited) {
     if (candidates->size() >=
@@ -68,14 +69,16 @@ bool ImmediatePrecedes(
         right.logical_ordinal;
 }
 
+template<class CandidateSequence>
 bool GenerateCandidates(
     const laplace_observation_query_index& index,
     const laplace_observation_query_binding& binding,
     const laplace_id128& entity_id,
-    std::vector<Candidate>* const candidates,
+    CandidateSequence* const candidates,
     std::uint64_t* const rows_examined,
     std::uint64_t* const index_plan_count,
-    bool* const limited) {
+    bool* const limited,
+    const std::size_t maximum_output_records = std::numeric_limits<std::size_t>::max()) {
     candidates->clear();
     *rows_examined = 0U;
     *index_plan_count = 0U;
@@ -88,6 +91,10 @@ bool GenerateCandidates(
         return ChargeWork(index, rows_examined, limited);
     };
     const auto append = [&](const Candidate& candidate) -> bool {
+        if (candidates->size() >= maximum_output_records) {
+            *limited = true;
+            return false;
+        }
         return AppendCandidate(index, candidates, candidate, limited);
     };
 
@@ -265,7 +272,9 @@ bool GenerateCandidates(
         candidates->clear();
         return true;
     }
-    std::sort(candidates->begin(), candidates->end(), CandidateLess);
+    if (candidates->size() > 1U) {
+        std::sort(candidates->begin(), candidates->end(), CandidateLess);
+    }
     return true;
 }
 
diff --git a/integrations/postgresql/extension/src/observation_cognition_persisted_pg.c b/integrations/postgresql/extension/src/observation_cognition_persisted_pg.c
index d6677cb4..4ae2a0b5 100644
--- a/integrations/postgresql/extension/src/observation_cognition_persisted_pg.c
+++ b/integrations/postgresql/extension/src/observation_cognition_persisted_pg.c
@@ -356,7 +356,7 @@ static int persisted_enumerate_impl(
     Datum args[3];
     Datum* source_values;
     Datum* physicality_values;
-    uint64_t max_rows = state->memory_limit / UINT64_C(2048);
+    uint64_t max_rows, candidate_workspace, frontier_workspace;
     uint64_t rows, row, vertices = 0u, encoded_bytes = 0u;
     uint64_t reserved;
     uint64_t before_operations = state->database_operations;
@@ -371,6 +371,18 @@ static int persisted_enumerate_impl(
         source_count > MaxAllocSize / sizeof(Datum) ||
         (uint64_t)source_count > state->memory_limit / UINT64_C(128))
         persisted_limit("frontier argument budget exhausted");
+    if (capacity > UINT64_MAX /
+            LAPLACE_COGNITION_OBSERVATION_CANDIDATE_WORKSPACE_MULTIPLIER /
+            sizeof(laplace_cognition_observation_candidate))
+        persisted_limit("candidate workspace overflow");
+    candidate_workspace = (uint64_t)capacity *
+        LAPLACE_COGNITION_OBSERVATION_CANDIDATE_WORKSPACE_MULTIPLIER *
+        sizeof(laplace_cognition_observation_candidate);
+    frontier_workspace = (uint64_t)source_count * UINT64_C(128);
+    reserved = persisted_add(candidate_workspace, frontier_workspace);
+    if (reserved >= state->memory_limit)
+        persisted_limit("candidate and frontier workspace exceeds provider grant");
+    max_rows = (state->memory_limit - reserved) / UINT64_C(2048);
     if (max_rows == 0u) persisted_limit("no physicality workspace");
     if (max_rows >= (uint64_t)LONG_MAX) max_rows = (uint64_t)LONG_MAX - 1u;
     source_values = palloc(source_count * sizeof(Datum));
@@ -415,8 +427,8 @@ static int persisted_enumerate_impl(
      * maps/vectors and result staging before detoasting trajectory bytes. The
      * caller separately reserves the native search's declared memory budget. */
     if (vertices > UINT64_MAX / UINT64_C(1024)) persisted_limit("vertex budget overflow");
-    reserved = persisted_add(rows * UINT64_C(2048), vertices * UINT64_C(1024));
-    reserved = persisted_add(reserved, (uint64_t)source_count * UINT64_C(128));
+    reserved = persisted_add(reserved, rows * UINT64_C(2048));
+    reserved = persisted_add(reserved, vertices * UINT64_C(1024));
     if (reserved > state->memory_limit || vertices > MaxAllocSize / sizeof(*segments))
         persisted_limit("selected trajectories exceed physical-provider workspace");
     segments = palloc0((size_t)vertices * sizeof(*segments));
diff --git a/integrations/postgresql/extension/tests/observation_cognition_contract.sql.in b/integrations/postgresql/extension/tests/observation_cognition_contract.sql.in
index 11104447..19e194a4 100644
--- a/integrations/postgresql/extension/tests/observation_cognition_contract.sql.in
+++ b/integrations/postgresql/extension/tests/observation_cognition_contract.sql.in
@@ -276,6 +276,7 @@ DECLARE q laplace.cognition_observation_request; changed laplace.cognition_obser
         search_limits laplace.cognition_observation_search_budget;
         forward_limits laplace.cognition_observation_forward_limits;
         observed laplace.cognition_observation_persisted_result; bad boolean;
+        small_context laplace.execution_context;
 BEGIN
     SELECT (request).* INTO STRICT q FROM observation_cognition_fixture;
     changed := q;
@@ -289,6 +290,17 @@ BEGIN
     EXCEPTION WHEN program_limit_exceeded THEN NULL;
     END;
     IF bad THEN RAISE EXCEPTION 'missing provider workspace was accepted'; END IF;
+    -- Metadata/decoded records alone fit here, but candidate staging does not.
+    -- That extra storage must be reserved before payload/index construction.
+    small_context := pg_temp.execution_context();
+    small_context.memory_bytes := (q.search_budget).max_memory_bytes + 6000;
+    bad := false;
+    BEGIN
+        PERFORM laplace.cognition_observation_execute_persisted(small_context, q);
+        bad := true;
+    EXCEPTION WHEN program_limit_exceeded THEN NULL;
+    END;
+    IF bad THEN RAISE EXCEPTION 'candidate staging escaped the provider memory grant'; END IF;
     changed := q;
     forward_limits := q.forward_limits;
     forward_limits.max_database_operations := 1;
diff --git a/tests/observation_query_tests.cpp b/tests/observation_query_tests.cpp
index f0b82cf2..725019aa 100644
--- a/tests/observation_query_tests.cpp
+++ b/tests/observation_query_tests.cpp
@@ -618,6 +618,15 @@ TEST(ObservationCandidateBatch, CapacityFailureDoesNotPublishAPrefix) {
     EXPECT_EQ(count, 0U);
     EXPECT_EQ(std::memcmp(&output, &sentinel, sizeof(output)), 0);
     EXPECT_EQ(usage.crossing_count, 0U);
+    std::array<laplace_cognition_observation_candidate, 16> complete{};
+    ASSERT_EQ(laplace_observation_query_index_candidates_batch(index.value, &binding,
+        &fixture.root, 1U, complete.data(), complete.size(), &count, &usage),
+        LAPLACE_OBSERVATION_QUERY_OK);
+    EXPECT_GT(count, 1U);
+    EXPECT_EQ(usage.crossing_count, count);
+    EXPECT_EQ(laplace_observation_query_index_candidates_batch(index.value, &binding,
+        &fixture.root, 1U, nullptr, 0U, &count, &usage), LAPLACE_OBSERVATION_QUERY_OVERFLOW);
+    EXPECT_EQ(count, 0U);
 }
 
 TEST(ObservationCandidateBatch, InvalidBindingAndImpossibleInputCountsAreRejected) {
@@ -627,6 +636,13 @@ TEST(ObservationCandidateBatch, InvalidBindingAndImpossibleInputCountsAreRejecte
     std::size_t count = 99;
     laplace_cognition_observation_candidate output{};
     laplace_cognition_observation_candidate_usage usage{};
+    const auto overflowing_workspace = SIZE_MAX /
+        LAPLACE_COGNITION_OBSERVATION_CANDIDATE_WORKSPACE_MULTIPLIER /
+        sizeof(laplace_cognition_observation_candidate) + 1U;
+    EXPECT_EQ(laplace_observation_query_index_candidates_batch(index.value, &binding,
+        &fixture.a, 1U, &output, overflowing_workspace, &count, &usage),
+        LAPLACE_OBSERVATION_QUERY_OVERFLOW);
+    EXPECT_EQ(count, 0U);
     binding.binding_fingerprint.bytes[0] ^= 1U;
     EXPECT_EQ(laplace_observation_query_index_candidates_batch(index.value, &binding,
         &fixture.a, 1U, &output, 1U, &count, &usage), LAPLACE_OBSERVATION_QUERY_BINDING_INVALID);

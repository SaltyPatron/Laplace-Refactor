@LP-TEST-HIGHWAY-REGISTRY-PARITY @LP-TEST-HIGHWAY-APPEND-ONLY
Feature: One generated typed numerical highway connects the whole machine

  Scenario: Every mirror carries the same coordinate authority
    Given grammar AST-role language script reference relation modality representation recipe model trust operation and effect coordinates
    When the highway is generated for native PostgreSQL SQL Csharp documentation and perfcache consumers
    Then every mirror has identical kinds scopes identifiers aliases versions and retirement state
    And renumbering reuse mirror drift and zero-versus-absence collisions prevent activation

@LP-TEST-HIGHWAY-EXTERNAL-REFERENCE-SEPARATION @LP-TEST-HIGHWAY-AUTHORITY-RELEASE-COLLISION
Feature: A numerical highway is not one global integer namespace

  Scenario: Equal local identifiers remain scoped
    Given local identifier 42 in two namespaces and two releases of one authority
    When each identifier is registered
    Then kind authority release namespace local identifier and version form distinct typed coordinates
    And only an explicit witnessed mapping may relate them
    But neither coordinate becomes content identity

  @LP-TEST-ADMISSION-SOURCE-TOPOLOGY @LP-TEST-ADMISSION-FIELD-NODE-DISPOSITION
  Scenario: Source reference topology preserves content and occurrence distinctions
    Given typed reference occurrences retain their source row field and exact value-content identities
    When one authority-release-namespace topology batch resolves their Highway coordinates and lifecycle dispositions
    Then equal scoped values converge on one coordinate while their source occurrences remain independently addressable
    And present retired unresolved and absent remain distinct exact outcomes
    And a field node cannot replace the referenced value identity
    But coordinate convergence does not assert a cross-namespace mapping

@LP-TEST-HIGHWAY-CROSS-LANGUAGE @LP-TEST-HIGHWAY-NONLANGUAGE-OPERATION
Feature: Highway routes rise to shared structure and lower to witnessed realization

  Scenario: Language is a realization dimension rather than an engine flag
    Given source-language syntax and reference occurrences linked to shared typed AST content relation and semantic-act coordinates
    When a query rises through shared coordinates and lowers through another language recipe
    Then the result uses witnessed target morphology syntax pragmatics and surface realization
    And no English string or reminted identifier is the intermediate authority

  Scenario: Recipes operators and effects use the same connective plane
    Given a grammar symbol recipe operation trust class model factor and authorized effect
    When one typed program composes their coordinates
    Then the engine resolves them through the same append-only scoped highway
    And treating the highway as a multilingual seed lookup fails acceptance

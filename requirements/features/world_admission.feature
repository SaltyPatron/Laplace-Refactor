@LP-TEST-ADMISSION-EVEN-PLAYING-FIELD @LP-TEST-ADMISSION-NO-TRAINING
Feature: Heterogeneous source admission supplies world state to the calculating machine

  Scenario: Unlike digital sources become comparable without model training
    Given exact text image audio video code interaction domain and model artifacts
    When their grammar and admission recipes execute through the common ISA
    Then their typed ASTs occurrences claims behaviors constraints and unknowns become SQL-addressable persistent state
    And evidence trust dependence contradiction context and time apply without privileging a source modality or model
    But no training campaign or GPU is required to make the admitted state interoperable and queryable

@LP-TEST-ADMISSION-NONEXEMPLAR @LP-TEST-ADMISSION-SOURCE-TOPOLOGY
Feature: The known witness universe cannot collapse to an exemplar

  Scenario: A single corpus grammar or modality cannot prove generic admission
    Given the selected standards grammar document lexical predicate bridge commonsense syntax multilingual interaction code media domain and model witness universe
    When one source such as WordNet PGN or a Tree-sitter grammar completes
    Then only that exact source-recipe boundary may be complete
    And generic capability still requires unrelated shapes providers grammars and modalities
    And configured foundational seed state still requires the whole selected heterogeneous receipted boundary

  @LP-TEST-ACTIVATION-STATE-SEPARATION
  Scenario: Source admission and foundational seeding are different product states
    Given an implementation has passed controlled integration outside the selected installed product
    And the selected installed product can separately activate a capability
    When one exact source profile completes its declared fidelity evidence and read-reachability boundary
    Then only that source-profile boundary is world admitted
    And the configured foundational heterogeneous boundary is seeded only after every selected profile cross-source dependence reconciliation and required derived closure completes
    And release remains a later complete-product package outcome
    But implementation integration product activation world admission seed completion and release cannot receipt one another

  Scenario: Source families do not form an admission waterfall
    Given two sources with independent recipes and a third mapping whose endpoints depend on both
    When the admission planner resolves exact artifact grammar reference and epoch dependencies
    Then compatible operations may interleave and each source may execute several recipes
    And no fixed family order or universal horizontal pass list defines semantics

@LP-TEST-ADMISSION-FIELD-NODE-DISPOSITION @LP-TEST-ADMISSION-READ-REACHABILITY
Feature: Every source distinction closes through AST and readback receipts

  Scenario: Curated distribution fidelity and selected content fidelity remain separate
    Given a curated source package whose archive layout is not selected as canonical world content
    And that package contains sentences words images audio video or other digital structures selected by its profile
    When the source recipe lowers the selected structures into the universal AST
    Then the package retains exact hash length authority release license and dependence provenance without claiming bit-reconstruction
    And every selected exact structure recomposes bit-for-bit from its persisted AST rather than from the original package
    And equal selected content from another source converges on the same content identity while its occurrence testimony and provenance remain distinct

  Scenario: Source closure includes syntax rather than only records
    Given a selected source artifact and exact expected byte record field node edge reference and occurrence denominators
    When admission completes
    Then every byte record field syntax node scanner output span ambiguity error unknown transformation and endpoint has one typed disposition
    And exact or declared semantic reconstruction plus reference reachability succeeds through public SQL

@LP-TEST-ADMISSION-DEPENDENCE @LP-TEST-ADMISSION-GRAMMAR-NONSEMANTIC
Feature: Sources and grammar providers remain witnesses rather than semantic authority

  Scenario: Copied and model-derived claims do not amplify support
    Given mirrors quotations shared datasets fine-tunes distilled models and independent observations
    When admission records their testimony and dependence
    Then shared roots remain explicit and adjudication can distinguish copied from independent support
    And no parser success row count or source reputation rewrites epistemic kind

  Scenario: A grammar recovers syntax but cannot decide truth or identity
    Given a parser emits a valid concrete tree and an intentional error node
    When the lowering recipe executes
    Then both structures retain exact source positions and typed dispositions
    And the provider cannot mint content identity assert interpretation discard the error or publish testimony by itself

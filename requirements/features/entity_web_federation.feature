@LP-TEST-APPLICATION-UNIVERSAL-PRIMITIVES @LP-TEST-APPLICATION-PRIVATE-ENGINE-REJECTION @LP-TEST-EXTERNAL-ACCOUNT-WITNESS
Feature: Application concepts remain modalities of one machine
  A person, organization, external account, project, activity, achievement, page, and
  node use the same substrate laws rather than installing a social, billing, résumé,
  or federation engine beside Laplace.

  Scenario: A person cannot collapse into a SaaS account row
    Given one person has witnessed Laplace, payment-provider, repository, school, employer, and service identities
    When their referential world is resolved
    Then exact names, account identifiers, mappings, activities, and claims remain distinct content occurrences and testimony
    And competing or expired account mappings remain historically addressable
    And the person referent is not minted from one provider identifier or stored as an opaque profile blob
    And every query and update executes through the common typed ISA and evidence laws

@LP-TEST-ENTITY-WEB-RECIPE-MATERIALIZATION @LP-TEST-ENTITY-WEB-VISIBILITY-NONIDENTITY @LP-TEST-ENTITY-WEB-CROSS-MODAL-PARITY @LP-TEST-ENTITY-WEB-REPLAY
Feature: A personal web is an authorized materialization of an entity world

  Scenario: One pinned personal-world recipe realizes several product surfaces
    Given a person world contains private shared and public authored activity relationship preference entitlement and achievement state
    And a versioned recipe selects and organizes only state authorized for one audience
    When Laplace materializes a profile feed page and portfolio as web mobile API and document results
    Then every surface executes the same query selection visibility and semantic program
    And each modality receives its own exact realization receipt
    And changing layout or visibility creates a new materialization without changing person identity testimony history or private state
    But no transport schema or page component may implement private identity evidence entitlement or query semantics

@LP-TEST-RESUME-WITNESSED-EVIDENCE
Feature: A résumé is a query-relative witnessed projection

  Scenario: A professional claim expands to its evidence rather than a keyword
    Given person employer project repository commit pull-request review issue release workflow artifact and deployment witnesses
    And Git author fields employer statements and self assertions have distinct authority and dependence
    When an authorized recipient requests a résumé for a declared role
    Then a recipe selects relevant claims with their time provenance independent roots contradictions and visibility dispositions
    And each displayed claim can expand to the exact supporting and competing evidence permitted to that recipient
    And forged squashed shared pair-programmed AI-assisted private or missing Git history remains representable
    But neither Git nor an employer receives exclusive authority over the person's history

@LP-TEST-ENTITLEMENT-TESTIMONY-GOVERNANCE-SEPARATION @LP-TEST-ENTITLEMENT-HISTORY-PRESERVATION @LP-TEST-ACHIEVEMENT-AUTHORITY-VISIBILITY @LP-TEST-SELF-ASSERTION-NONAUTHORITY
Feature: Entitlements and achievement cookies are governed witnessed relations

  Scenario: An external membership changes current capability without changing identity or history
    Given an authenticated provider asserts a time-bounded membership and a separate authority witnesses an achievement
    And the person may also make a self assertion about that achievement
    When the active governance recipes calculate current entitlement capability and public display
    Then provider authority time dependence and visibility remain explicit
    And current capability can expire while historical testimony remains queryable
    And the external membership and achievement change neither person identity nor epistemic standing by themselves
    And the self assertion cannot impersonate the independent achievement authority

@LP-TEST-NODE-SAME-MACHINE-SEMANTICS @LP-TEST-NODE-PHYSICAL-PLAN-PARITY @LP-TEST-NODE-SUPPORT-CLAIM-GATE @LP-TEST-NODE-PRIVATE-ENGINE-REJECTION
Feature: Hardware capacity cannot create another Laplace architecture

  Scenario: Node support requires the same machine acceptance
    Given server desktop and ARM Raspberry Pi candidates with different CPU memory storage and acceleration resources
    When each candidate derives a physical plan for the same pinned typed program
    Then any supported target returns the same identities logical results evidence dispositions and receipt meaning
    And only capacity timing placement and validated provider selection may differ
    And ARM Raspberry Pi remains unsupported until its exact ABI package crash recovery semantic parity and representative product tests pass
    But no target may receive a simplified private semantic engine or a separately defined Pi version of Laplace

@LP-TEST-FEDERATION-SCOPE-AUTHORITY @LP-TEST-FEDERATION-CONTENT-RECEIPT-INTEGRITY @LP-TEST-FEDERATION-PROVIDER-NONAUTHORITY @LP-TEST-FEDERATION-REPLAY-CONVERGENCE
Feature: Federation preserves user-controlled world state and machine law

  Scenario: A personal node synchronizes an authorized projection without surrendering identity
    Given a node owns private shared and public scopes plus content-addressed receipts
    And discovery relay backup billing and availability services are replaceable external providers
    When an authenticated peer requests an authorized shared projection
    Then the exchange binds exact content AST testimony provenance authority recipe epoch and visibility receipts
    And denied or private state is neither transmitted nor inferred as absent
    And replay of the same accepted exchange converges without reminting content or rewriting testimony
    But no central provider becomes owner of the person's identity truth firmware or semantic execution

@LP-TEST-PLACEMENT-SEMANTIC-PARITY @LP-TEST-REMOTE-POSTGRES-EXECUTION-BOUNDARY @LP-TEST-POSTGRES-STORAGE-DURABILITY @LP-TEST-PLACEMENT-RECEIPT
Feature: Execution and storage placement are typed physical providers

  Scenario: Disaggregated placement preserves semantics and reports where execution occurred
    Given local native execution remote PostgreSQL canonical storage archive storage perfcaches source APIs and federation peers
    When the planner selects a valid distributed physical plan
    Then the logical program result and evidence laws match a valid colocated plan
    And the receipt identifies every execution storage network consistency durability and cache boundary
    And remote PostgreSQL is reported as the location of its server-owned semantic operations
    And storage backing PostgreSQL activates only after locking atomicity fsync and crash-durability acceptance

**Stockfish persisted syntax diagnostics — reviewed 2026-09-17**

The authenticated extraction contains **102 ERROR/MISSING nodes across 24 files: 58 ERROR and 44 MISSING**. This record preserves the source-context partition of those existing rows. Subsequent hosted execution confirms the three initial grammar corrections and the pointer-member correction. The latest qualified derivative reconstructs the complete source corpus and reports **95 native diagnostics**, compared with 96 for the earlier three-rule derivative. These separate native plans do not alter this historical persisted profile. **`executable_semantics_verified` remains `false`.**

The [complete classification record](STOCKFISH_SYNTAX_DIAGNOSTICS.json) contains every row's artifact index, span index, path, source line, half-open UTF-8 byte range, syntax flags, node/parent/grandparent grammar kinds, and applicable finding. Its `node_columns` names the compact row fields. Coordinates are scoped to the authenticated source profile; they are not new canonical entity IDs. Grammar-kind integers outside JavaScript's exact safe range are preserved as decimal strings.

The input is `diagnostics.json` from [extraction run 35168832815](https://github.com/SaltyPatron/Laplace-Refactor/actions/runs/35168832815), workflow commit `557d468b41b1a42c66dbb35e795b45e009ce9ae6`, artifact **10476716479**, **331,918 bytes**, archive SHA256 **`f857f717882a5310f4828bfcc564b0fa0b29ffb3ebac2496d98521fb68bd7386`**. [Hosted reader job 105043502755](https://github.com/SaltyPatron/Laplace-Refactor/actions/runs/35171351580/job/105043502755) authenticated the producer run, artifact metadata, exact archive size and SHA256 before emitting `PERSISTED_SYNTAX_DIAGNOSTICS`. The JSON record separately identifies the emitted JSON hash; it does not present that hash as the raw artifact member's digest.

The source is official Stockfish commit `edb0d9db6731067ec50ce619ff372b463bc4dd5d`, profile `2368be2a20fd01a5cdfca1f90a79b923741e521e64718ae996a70af98f4ec651`, observed in installed package `912e2628fb504b66ba3cd8da48957e781650622771c826804cdc491ae71202fe`. The frozen manifest SHA256 is `2346d3970f5ae3619b7ad69bbc80939c816bb14af94249be5c20a44c5ac33adc`. Extraction used a repeatable-read, read-only PostgreSQL transaction; installed activation was unchanged.

| Source-context group | Rows | ERROR | MISSING |
|---|---:|---:|---:|
| Macro expansion context | 55 | 31 | 24 |
| Preprocessor or macro-definition structure | 13 | 11 | 2 |
| GNU extension recovery | 26 | 10 | 16 |
| Ordinary C++ recovery | 8 | 6 | 2 |
| **Total** | **102** | **58** | **44** |

The macro group includes unexpanded annotation, statement, declaration and string macros. The preprocessor group includes conditional directives inside declarations/statements, `#embed`, and macro definitions/token pasting; three rows occur inside `RVV_SPARSE_PROPAGATE`. These labels describe observed context. They do not establish that preprocessing would resolve every row or that the rows are harmless.

The review uses upstream tree-sitter-cpp commit **`8b5b49eb196bec7040441bee33b2c9a4838d6967`**, tree `d5ed868917a07bcc8272f37e551d707e7a836f95`, selected by the [frozen Refactor grammar lock](https://github.com/SaltyPatron/Laplace-Refactor/blob/b042de4216e1d674effe5bd3e8ecf3ebc7aeddac/dependencies/tree-sitter-grammars.lock.json). Exact bytes of both reviewed files were hashed and their Git blob hashes matched to that upstream commit tree:

| Primary source | Bytes | SHA256 | Git blob SHA1 |
|---|---:|---|---|
| [grammar.js](https://github.com/tree-sitter/tree-sitter-cpp/blob/8b5b49eb196bec7040441bee33b2c9a4838d6967/grammar.js) | 43,970 | `4545b6bf72e8a38d3e9a6c9a3deb9dbce37dfce357133c1b9e3a172831234622` | `ffbaf0f13b6bfd756635f50a484192f35f6fdd0e` |
| [src/grammar.json](https://github.com/tree-sitter/tree-sitter-cpp/blob/8b5b49eb196bec7040441bee33b2c9a4838d6967/src/grammar.json) | 458,800 | `3aa14b0b6ef87951ef9933d126f6d9774bc7d7dba11e7be1861820e381d36455` | `a091cbff0611375b03e738af6a438df6010ba9c3` |

The lock's parser, scanner and archive SHA256 values are retained separately in the JSON with their provenance. Those large upstream inputs were not independently rehashed during the initial static review. Subsequent authenticated generation and canonical provider qualification verify their exact source identities, as recorded in [the provider qualification evidence](STOCKFISH_CPP_PROVIDER_QUALIFICATION.json).

| Ordinary C++ location | Coordinate (artifact, span) | Finding |
|---|---|---|
| `src/attacks.h:203` | `(39, 1551)` | The standard `for` condition admits expressions/comma expressions or empty, but not the declaration condition `Bitboard dest = …`. |
| `src/movegen.h:46` | `(57, 112)` | The conversion-operator declaration admits `= expression`; it does not use the deleted-method clause required by `operator float() const = delete;`. |
| `src/thread.h:175` | `(94, 1132)` | The historical and three-rule parsers produce one ERROR for this expression. The later pointer-member derivative parses the exact expression without ERROR/MISSING and passes explicit precedence controls. |
| `src/uci.cpp:54` | `(103, 172)` | The `using` declaration rule has no pack-expansion ellipsis for `using Ts::operator()...;`. |
| `src/attacks.h:205,214,215` | `(39, 1574)`, `(39, 1605)`, `(39, 1617)` | Possible recovery cascade from the line-203 condition. These are not three separately established defects. |
| `src/position.cpp:1549` | `(83, 13562)` | Unresolved recovery involving assignment and a comma expression. Both productions exist in the grammar; the root cause still needs an isolated reproduction. |

The GNU group contains 24 rows around eight aliases using `__attribute__((may_alias))`. The alias rule accepts standard attribute declarations, while GNU attributes have a separate rule. Two further rows occur in `__extension__` aliases for signed/unsigned `__int128`; the compound extension/alias cause is not yet isolated.

The three-rule derivative corrects `_for_statement_body`, `operator_cast_declaration` and `using_declaration`. Actual generation at [run 35181390710](https://github.com/SaltyPatron/Laplace-Refactor/actions/runs/35181390710) passed the unchanged inherited C corpus (85 cases) and C++ corpus (198 cases each for the locked, regenerated-baseline and candidate variants). The authentic declaration-condition, deleted-conversion and using-pack snippets became error-free; malformed controls retained diagnostics.

The earlier three-rule experiment passed **unchanged-CST parity**, which means the candidate preserved the original parse trees. It did not establish valid parsing of those expressions. The authentic `->*` snippet and seven other pointer cases retain ERROR/MISSING in all three variants; only one of nine pointer cases is error-free. No pointer-member grammar rule was changed, and no general precedence correctness is claimed.

The earlier three-rule dependency is [commit `8ece5afbc1e97f8d4344355b15822110e8231056`](https://github.com/SaltyPatron/Laplace-Refactor/commit/8ece5afbc1e97f8d4344355b15822110e8231056), tree `cd00239b00007a65e70fde9c8163233e5f651095`, measured Git archive SHA256 `e0be65c894548c136ad059942375d89750381a6d3a2138cd383eb70b213fcb9f`. Its tracked provenance preserves the official upstream base, exact patch, generator and generated bytes. The dependency tree has an actual operational parent; it does not claim official fork ancestry.

[Canonical hosted qualification 35186357806](https://github.com/SaltyPatron/Laplace-Refactor/actions/runs/35186357806) passed with the unchanged provider build owner and selected Tree-sitter **v0.26.13**. Both registered native contracts passed. The unchanged official provider passed the four existing baseline cases and then failed the new declaration-condition regression with expected status 16. The complete candidate source plan reconstructed **119 files / 1,172,144 bytes**, including **72 C++ files**, with **96 ERROR/MISSING nodes**, 197,083 witnesses and 60,766 requests. Its 2.065-second measurement covers native decomposition and reconstruction; it is not recorded chess-game throughput.

The successful native artifact is **10481758203**, **19,735,930 bytes**, SHA256 **`d3869e90096cd21dcccf37d0e44c46363bfe81f8ace12cd6272e2f868d69af82`**. [Readback run 35186585000](https://github.com/SaltyPatron/Laplace-Refactor/actions/runs/35186585000) authenticated the full archive, proposal, local candidate identity, provider libraries, native test receipts, runtime objects and exact reconstruction evidence. The actual runtime uses 13 separate C translation units; compilation receipts preserve all 13 source/object identities.

The earlier three-rule v2 grammar-lock proposal is Git blob `0433ef054fd5ff21977616688ce0087f741cdcb4`, SHA256 `8978e6b56b72c174436529f4e3990b3a08ae3364aaf73df860e4df793699d5a3`; the other 299 entries are unchanged. Qualification executed a clean local two-leaf candidate containing that lock and the native regression probe. This evidence does not itself select or install the provider.

The completed pointer-member derivative is [commit `94fe0c1ba60dee178a4782c1082441631638f51b`](https://github.com/SaltyPatron/Laplace-Refactor/commit/94fe0c1ba60dee178a4782c1082441631638f51b), tree `b33986de36171b1766ab39282dffb0dc97f66e2c`, with Git archive SHA256 `048dd9e42051ac037c87bfcefd9d3966f4b052b5dfef811b9b862e564c3204ae`. It is a child of the earlier three-rule dependency and retains that provenance. It adds explicit `.*`/`->*` expressions with cast and multiplication precedence, left associativity, and pointer-member assignment support. One existing corpus expectation changes from a field-expression tree to a binary-expression tree for `(obj.*member_function_ptr)(42)`; every source byte in that fixture is unchanged. [Actual syntax run 35194152219](https://github.com/SaltyPatron/Laplace-Refactor/actions/runs/35194152219) passed all 85 inherited C cases, all 198 C++ cases for both baseline and candidate, and all 50 explicit assertions, including 18 pointer-member shape checks.

[Selected native-runtime qualification 35195112861](https://github.com/SaltyPatron/Laplace-Refactor/actions/runs/35195112861) compiled all 13 actual Tree-sitter v0.26.13 translation units and passed both native contracts. Its probe includes five accepted regression snippets and four malformed regression controls, in addition to the four original controls. The official-provider counterexample still returns expected status 16 after those four original controls. The complete native plan reconstructed all **119 files / 1,172,144 bytes**, with **95 ERROR/MISSING nodes**, 197,083 witnesses and 60,766 requests. The 3.487-second elapsed observation is neither a recorded-game rate nor a controlled speed comparison. Artifact **10485363567** is **19,702,033 bytes**, SHA256 **`9658bb0e4c8827561801afd244d79ca101aaf6db4357de75c07105f2145a149d`**. [The exact pointer qualification record](STOCKFISH_CPP_POINTER_MEMBER_QUALIFICATION.json) binds the source, archive, native library, provider and probe identities.

The latest qualified lock is Git blob `eaebd9a8cba151e98d25fde8442adc5258f55f1f`; the other 299 grammar entries remain unchanged. Qualification used a clean local candidate over installed source `af0f3cf48de13005875f78888271ec8ccdf332c4`, changing only that lock and the native regression probe. This proof does not itself activate the provider or admit a new PostgreSQL source profile. The existing installed-profile witness replay mismatch is diagnosed before changing the provider, so a new grammar/profile identity cannot conceal it.

**Historical persisted count: 102. Earlier three-rule native count: 96. Latest pointer-member native count: 95.** These are separately scoped observations. The remaining 95 coordinates have not been enumerated by this qualification. Possible cascades, the enclosing `if` condition and GNU alias forms still require precise follow-up; no remaining group is declared harmless.

Counts describe recovery nodes, including nested or cascading recovery, rather than independent source defects. The profile's persisted `unsupported_count=0` and `unknown_count=0` do not establish support for every valid C++ construct. This record neither proves executable semantics nor changes canonical source/entity identities, physical source bytes, or database records.
